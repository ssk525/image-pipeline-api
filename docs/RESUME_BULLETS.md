# Resume bullets — Image Pipeline API

Keep **max 3 lines**. Tailor keywords lightly (FastAPI, OpenCV, REST, Docker).

## Default (CV + SWE)

- Built a FastAPI image processing microservice with OpenCV: resize, format conversion (JPEG/PNG/WebP), and filters (sharpen, denoise, equalize) over REST multipart uploads.
- Exposed latency and size metrics via response headers; added Pytest coverage and Docker Compose for reproducible local deploy.
- Tuned encode quality and geometry (`max_side` thumbnails) for compact outputs suitable for camera / media pipeline demos.

## MathWorks / Qualcomm lean

- Implemented an OpenCV-backed image transform API (geometry + filtering + encode) with measured process latency for embedded / camera-pipeline discussions.
- Packaged as a Dockerized FastAPI service with automated tests and benchmark script for before/after size and timing comparisons.

## Skills line

`Python · FastAPI · OpenCV · REST APIs · Docker · Pytest · Image Processing · Linux`
